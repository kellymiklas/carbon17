/*
 * created by Kelly Miklas (7 June 2017)
 */

 // Check if face detected
var check_for_face = function() {
	// get video feed
	var video = document.getElementById('webgazerVideoFeed');

	// resolve frames: get ___ # of frames from ___ secs of video

	// use API to analyze features

	// Query DB -> compare features
}


 // If over ____% accurate, successful

 // Else if failed once, remind user to look into camera, take off glasses, non-religious headwear, etc (to match license)

 // Else if failed a second time, freeze card and prevent user from attempting PIN